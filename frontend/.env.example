# Backend (Laravel) API base URL.
# - Leave empty or unset: use relative /api (same origin or Vite proxy in dev).
# - Set to full URL: e.g. http://localhost:9090 or https://api.yourdomain.com
VITE_API_URL=
